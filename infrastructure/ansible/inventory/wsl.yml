---
# WSL cluster inventory — Talos in Docker mode on the desktop workstation.
# All operations run against localhost.

all:
  vars:
    talos_target: wsl
    cluster_name: homelab-wsl
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
