---
# Local cluster inventory — Talos in Docker mode on this workstation.
# All operations run against localhost.

all:
  vars:
    talos_target: local
    cluster_name: homelab-local
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: "{{ ansible_playbook_python }}"
