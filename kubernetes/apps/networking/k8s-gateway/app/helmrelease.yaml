---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: k8s-gateway
  namespace: networking
spec:
  interval: 15m
  chart:
    spec:
      chart: k8s-gateway
      version: 2.4.0
      sourceRef:
        kind: HelmRepository
        name: k8s-gateway
        namespace: flux-system
  maxHistory: 2
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    fullnameOverride: k8s-gateway
    # Advertise both the external (CLUSTER_DOMAIN) and internal (INTERNAL_DOMAIN)
    # zones from the cluster. In prod these resolve different DNS suffixes;
    # in local they're the same value (tnwks.local).
    domain: "${CLUSTER_DOMAIN}"
    extraArgs:
      - --domain=${INTERNAL_DOMAIN}
    ttl: 1
    service:
      type: LoadBalancer
      port: 53
      # Both Cilium LB-IPAM (MS-01) and MetalLB (WSL) honor `loadBalancerIP`
      # in the Service spec. Cilium also accepts the
      # `io.cilium/lb-ipam-ips` annotation; MetalLB ignores it. Using the
      # spec field keeps a single value working across both clusters.
      loadBalancerIP: "${METALLB_K8S_GATEWAY_ADDR}"
      externalTrafficPolicy: Local
